This copy of the intepreter source code came primarily from the INTERP floppy disk.  However, FPI.TEXT, SET1.TEXT and STP.TEXT were added from the OS floppy, because I ran into disk errors while downloading those files from INTERP.

